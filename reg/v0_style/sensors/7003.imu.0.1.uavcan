float32[3] delta_angle       # radians
float32[3] delta_velocity    # m/s

uint16 delta_angle_dt        # delta angle (gyroscope) integration time in microseconds
uint16 delta_velocity_dt     # delta velocity (accelerometer) integration time in microseconds

uint3 instance

@extent 63 * 8               # Single-frame transfer over CAN FD.
